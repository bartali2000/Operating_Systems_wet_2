#pragma once
#include <pthread.h>
#include <vector>
#include <queue>  // for jobs
#include <deque> // for the snapshots
#include <map> // for the accounts
class Bank {
    int vip_threads;
    int threads;
    public:
};